#[allow(unused_variables)]
const URL: &str = "google.com";

fn main() {
    // INFO: CONSTANTS | UPPERCASE | DATA TYPES MANDATORY
    // INFO: Global scope, accessible from anywhere or local scope, accessible only within the
    // block it is defined in
    println!("🪚 URL: {}", URL);
}
